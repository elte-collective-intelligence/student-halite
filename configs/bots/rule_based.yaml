name: "RuleBased"                    # Bot name identifier
base_strength: 60                    # Base strength threshold for movement decisions
scale: 10                            # Scaling factor for strength calculations
max_search: 4                        # Maximum search depth for pathfinding
production_enemy_preference: 0.9     # Preference weight for targeting enemy production cells (0.0 to 1.0)

